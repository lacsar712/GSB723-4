# ---------------------------------------------------------------------------
# Maven 常用参数固化（位于 backend/.mvn/maven.config，会被 Maven 自动加载）
#
# 约定：
#   * -T 1C               并行构建：每个 CPU 核心 1 个线程；
#   * -Dspring.profiles.active=test
#                         让 Surefire 启动的 Spring 上下文默认激活项目自带的
#                         `test` profile（H2 内存库 + simple cache，不连真实
#                         Redis/MySQL）；与 @ActiveProfiles("test") 双保险；
#   * --batch-mode        CI / 脚本化执行友好，不输出彩色交互进度。
#
# 如需覆盖默认 profile，可在命令行显式追加，例如：
#   mvn spring-boot:run -Dspring.profiles.active=docker
# ---------------------------------------------------------------------------
-T 1C
-Dspring.profiles.active=test
--batch-mode
